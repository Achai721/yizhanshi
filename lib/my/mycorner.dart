import 'package:flutter/material.dart';

class mycorner extends StatelessWidget {
  const mycorner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
